import 'package:flutter/material.dart';

class TodoViewScreen extends StatelessWidget {
const TodoViewScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}